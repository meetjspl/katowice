---
title: 'Prelegenci'
date: 2017-02-17 13:04:11
---
# Prelegenci

Tutaj możesz znaleźć listę naszych prelegentów i wygłoszonych przez nich prezentacji oraz odnośnikami do przedstawionych na spotkaniu materiałów.

<% speakers.sort((a, b) => {
  let nameA = `${a.lastName} ${a.firstName}`;
  let nameB = `${b.lastName} ${b.firstName}`;

  if (nameA < nameB) return -1;
  if (nameA > nameB) return 1;

  return 0;
}).forEach((speaker) => { -%>
## <%= [speaker.firstName, speaker.lastName].join(" "); -%>
<% if(speaker.urls) { -%>
<% if(speaker.urls.web) { -%> {% icon_link globe <%= speaker.urls.web %> %}<% } -%>
<% if(speaker.urls.github) { -%> {% icon_link github https://github.com/<%= speaker.urls.github -%> %}<% } -%>
<% if(speaker.urls.twitter) { -%> {% icon_link twitter https://twitter.com/<%= speaker.urls.twitter -%> %}<% } -%>
<% } -%>


### Prezentacje

<% speaker.talks.sort((a, b) => {
  if (a.meetup < b.meetup) return -1;
  if (a.meetup > b.meetup) return 1;

  return 0;
}).forEach((talk) => { -%>
* {% post_link meetjs-katowice-<%= talk.meetup %> #<%= talk.meetup %> %} (<%= talk.date %>) - *<%= talk.title %>*
<% if(talk.urls) { %>Materiały:<% if(talk.urls.slides) { -%> {% icon_link external-link <%= talk.urls.slides %> Slajdy %}<% } %><% if(talk.urls.video) { -%> {% icon_link youtube-play <%= talk.urls.video %> Nagranie wideo %}<% } %><% if(talk.urls.code) { -%> {% icon_link code <%= talk.urls.code %> Kod źródłowy %}<% } %><% } %>
<% }); %>
<% }); %>
